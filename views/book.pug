extends layout
 
block content
  h1= title
 
  div.container-fluid
    div.row
      -
        let book=results
       
      each book,index in results
        .col-sm-2
        div.col-sm-2(onclick=`buy(${index})`)
       
          p Title: #{book.book_title}
          p Author : #{book.book_author}
          p Price: $#{book.book_price.toFixed(2)}
 
  a(href="/") Back to index
 
    script.
        function buy(index) {
            alert(`You purchased item ${index}`);
        }
